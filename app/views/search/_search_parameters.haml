.row-fluid
  .span12.well
    %h4 Search Parameters
    %ul
      - if @parameters
        - @parameters.each do |param|
          %li.search-parameter
            %label= param[:name]
            = "#{param[:value]}°"
